#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print all numbers between 0 and 9
 *
 * Return: Always 0 (Success)
*/
int main()
{
    int number = 0;

while (number != 10)
{
    printf("%d", number);
    number++;
}

printf("\n");

    return (0);
}
